# 1. نستخدم نسخة بايثون خفيفة جداً (Alpine أو Slim)
FROM python:3.9-slim

# 2. نحدد مسار العمل داخل الحاوية
WORKDIR /app

# 3. ننسخ ملف الكود من جهازك إلى داخل الحاوية
COPY app.py .

# 4. الأمر الذي سيعمل بمجرد تشغيل الحاوية
CMD ["python", "app.py"]